# V26 스킬 점수 계산기

타자/투수 카드의 스킬 조합 점수를 계산하고,
카드 타입별 기준 확률과 판정 등급을 확인하는 웹 계산기입니다.

## 실행 방법

```bash
npm install
npm run dev
```

빌드/검증:

```bash
npm run build
npm run lint
```

---

## 작업 기록

### v0.4.0

- 투수 데이터셋 구조 확장
  - `pitcher/starter`, `pitcher/middle`, `pitcher/closer` 분리
  - 보직별 `skills.ts`, `skillScores.ts`, `cardThresholds.ts` 구조 통일
- 선발/중계/마무리 스킬 점수표 반영
- 카드 타입별 기준 확률 테이블 반영
- 국가대표 전용 스킬은 국가대표 카드에서만 노출되도록 메타 적용
- 스킬명 정리
  - 표시명에서 `★` 및 옆 숫자 제거
- 결과 문구 개선
  - `기준 밖` → `기준표 최저점 미만`
  - 최상위 구간 보조 멘트(`사기꾼`) 추가
- 초기화 동작 개선
  - 현재 선수 유형(타자/투수) 유지
- 스킬 검색 UX 개선
  - 검색 결과 클릭 적용 안정화(단일 결과 선택 버그 수정)
  - 결과 리스트 높이 고정
- 전체 UI 리뉴얼
  - 앱 쉘(헤더/기능 탭/입력·결과 패널) 구조 적용
  - 공통 스타일 변수 기반으로 레이아웃/톤 정리

### v0.1.0

- Vite + React + TypeScript 프로젝트 초기 세팅
- 기본 계산기 UI 구성
- 카드 종류 선택 기능 추가
- 스킬 3개 선택 기능 추가
- 스킬 레벨 선택 기능 추가
- 총점 계산 기능 구현
